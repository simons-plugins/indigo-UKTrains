# Test dependencies for UK-Trains Indigo Plugin

# Core testing frameworks
pytest==7.4.3
pytest-mock==3.12.0
pytest-cov==4.1.0

# Time mocking for testing time-dependent code
freezegun==1.4.0

# Coverage reporting
coverage[toml]==7.4.0

# Production runtime deps the plugin imports at test time
# (the new REST client is stdlib-only, so no zeep / requests / lxml)
pytz==2023.2
pydantic>=2.10.0,<3
tenacity==8.2.3
Pillow>=10.0.0
